import styles from './About.module.css'

const About = () => {
    return (
        <div>
            <h2 className={styles.heading}>it is about page</h2>
        </div>
    );
};

export default About;